import React from "react";
import { Text, View } from "react-native";

const User = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 40, fontWeight: "bold", color: "#006600" }}>
        HomeScreen
      </Text>
    </View>
  );
};

export default User;
